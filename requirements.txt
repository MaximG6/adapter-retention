# Pinned to the versions every number in the paper was produced with.
# See paper/appendix-D-reproduction.md.
#
# torch is NOT listed here: it must be installed first, from the CUDA-matched index,
# because a mismatched build imports cleanly and then produces wrong numbers on
# Blackwell rather than failing. Install it with:
#
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128
#
# then `pip install -r requirements.txt`.

transformers==5.14.1
peft==0.20.0
trl==1.9.2
datasets==5.0.1
accelerate==1.14.0
numpy==2.4.6
pydantic==2.13.4
safetensors==0.8.0
huggingface-hub==1.25.1

# Test and analysis
pytest==9.1.1

# Figures and document builds. Omitting these was a real defect: Appendix D tells a
# reader to install from this file, and without them every figure script and both PDF
# builds fail on import.
matplotlib==3.11.1
markdown==3.10.3
pypdf==6.14.2
